import styled from "styled-components";

export const CodWrapper = styled.section`
  overflow: hidden;
  gap: 10px;
`;

export const ContentrSlideWrapper = styled.div`
  overflow: hidden;
  position: relative;
  display: flex;
  gap: 15px;
  margin: 1rem 0;
  padding: 1rem 0;
  flex-wrap: nowrap;
`;
