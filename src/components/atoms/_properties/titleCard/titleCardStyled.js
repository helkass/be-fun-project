import styled from "styled-components";

export const TitleCardWrapper = styled.p`
  font-weight: 600;
  font-size: 0.9rem;
`;

export const DescCard = styled.p`
  text-overflow: ellipsis;
  max-height: 3rem;
  font-size: 0.8rem;
  font-weight: 500;
  overflow: hidden;
  white-space: nowrap;
`;
